name: ct_micro_commons_shared
description: 
version: 0.0.1
homepage:

environment:
  sdk: '>=2.18.2 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dependency_overrides:
  ct_micro_commons_dependencies:
    path: ../ct_micro_commons_dependencies
    # git:
    #   url: "https://github.com/cuiatech/ct_micro_commons_dependencies.git"
    #   ref: "master"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/icons/
    - assets/images/
    - assets/lang/
